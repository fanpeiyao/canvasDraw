functionList.toSearch = function(obj,type){
    obj = jQuery(obj);
    if(type == 'focus' && obj.val() == '支持模糊查询'){
        obj.val('');
        obj.css("color","black");
    }
    if(type == 'blur' && obj.val() == ''){
        obj.val('支持模糊查询');
        obj.css("color","grey");
    }
};